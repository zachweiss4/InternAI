// @:user-owned — HTML locale read by the framework layout for <html lang>/dir.
// Set `lang` (e.g. 'fr') and `dir: 'rtl'` for RTL languages. Edit freely.

export const locale: { lang: string; dir: 'ltr' | 'rtl' } = {
  lang: 'en',
  dir: 'ltr',
};
